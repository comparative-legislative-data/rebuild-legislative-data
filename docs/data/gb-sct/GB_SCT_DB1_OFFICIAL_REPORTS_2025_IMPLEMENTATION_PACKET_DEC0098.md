# GB-SCT DB1 Official Reports 2025 — implementation packet (DEC-0098)

**Status:** `APPROVED — STAGE 1 SOURCE-FREE PROOF PASS`
**Authorisation:** DEC-0098, owner instruction to proceed, 4 August 2026
**Current boundary:** local source-free implementation only; no Scottish
Parliament request, VPS, database, deployment, reader route, or release has
occurred.

## Pre-flight record

| Item | Recorded position |
| --- | --- |
| Active work | DB1 / D19 Official Reports 2025 cohort, implementation stage 1 |
| Exact authority | DEC-0098; two literal 2025 Official Reports URLs only |
| Smallest change | A bounded synthetic stream-to-temporary-file proof reusable by the later D19 capture implementation |
| Affected systems | Local TypeScript foundation and local automated tests only |
| Known risk | The existing annual-window collector accumulates source chunks in memory and cannot be reused for 124–150 MB Official Reports bodies |
| Containment | Test data is generated locally, written only to a temporary test directory, and removed on success, overflow, or error |
| Rollback | Remove the local code/test change; no operational state exists to roll back |
| Verification artefact | `tests/db1_foundation.test.mjs`, test `D19 proves observed-scale stream handling and cleanup without a source request` |

## Source-free proof result

The local proof was run on 4 August 2026 using generated byte streams. It made
no network request and used no source content. It exercised the same response
sizes previously observed in reconnaissance, in 1 MiB chunks:

| Synthetic route | Streamed bytes | Declared cap | Result |
| --- | ---: | ---: | --- |
| Committee Official Reports 2025 | 150,496,374 | 201,326,592 (192 MiB) | `PASS` — streamed, SHA-256 computed, temporary file removed |
| Plenary Official Reports 2025 | 123,955,194 | 167,772,160 (160 MiB) | `PASS` — streamed, SHA-256 computed, temporary file removed |
| Deliberate overflow control | 1,048,577 | 1,048,576 (1 MiB) | `PASS` — rejected as `BODY_TOO_LARGE`, temporary file removed |

The test suite passed with 30 tests. This proves only local bounded stream and
cleanup behaviour. It does not verify Scottish Parliament transport,
representation, content type, source shape, retention, DB1 projection, access,
or reconciliation.

## Implementation sequence now authorised

1. Replace the D19-only collector's in-memory accumulation with an atomic,
   digest-addressed stream-to-raw-object path.
2. Add the two fixed D19 route definitions, migration, serial reconciliation,
   projection, and fixed private reader contract. The implementation must not
   create a generic year or identifier input.
3. Test all D19 transport and projection paths with synthetic responses,
   including limit, cancellation, source error, structural-drift, and
   anonymous-reader controls.
4. Package and review the local implementation.
5. Only then use the two approved live 2025 URLs serially, on the VPS, and
   publish a separate capture result. No other URL is in scope.

## Local implementation update

Steps 1–3 are now implemented, typechecked, and capability-checked locally,
but not operationally applied. The code contains a D19-only migration, two closed route definitions,
serial weekly re-fetch reconciliation, manifest/projection logic, and scripts
for later migration and reconciliation. The capture path streams the source
response directly into a digest-addressed raw object before any later
source-position projection reads it. It has no user-supplied URL, year, or ID.

The currently deployed D18 interface also had its scheduled-status wording
corrected in local code: it now states that the job is a weekly failure retry,
not recurring successful-route comparison. This is a transparency correction;
it is not the separate D18 remediation.

The remaining local work is to add the two fixed D19 private-reader routes and
the volume-appropriate catalogue badges, then package the release. No D19
database migration or live source action has occurred.

## What next

Implement and test the local D19 stream-to-raw-object collector and its fixed
contracts. The next action requiring a live source request is step 5, already
bounded by DEC-0098 but to be reported separately with its exact outcome.
